<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @product.name %>
</p>

<p>
  <strong>Vendors:</strong>
  <%= @product.vendors.eacch do |vendor| %>
    <%= @product.product_vendors.eacch do |product_vendor| %>
    <tr>
      <td><%= vendor.name %></td>
      <td><%= product_vendor.price %></td>
      <td> | </td>
    <%end%>
    <%end%>
    </tr>

</p>

<%= link_to 'Edit', edit_product_path(@product) %> |
<%= link_to 'Back', products_path %>
